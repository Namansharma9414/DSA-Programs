int isPlaindrome(string S)
	{
	      
    int n = S.length()-1;
    for(int i=0;i<S.length();i++){
        
        if(S[i]!=S[n-i]){
            return 0;
        }
        
        
    }
    return 1;
	}
